<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed - FitCart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card border-0 shadow-sm">
                    <div class="card-body text-center p-5">
                        <div class="mb-4">
                            <i class="bi bi-x-circle-fill text-danger" style="font-size: 3rem;"></i>
                        </div>

                        <h2 class="card-title text-danger mb-3">Payment Failed</h2>
                        <p class="text-muted mb-4">We were unable to process your payment successfully.</p>

                        <div class="bg-light rounded p-4 mb-4">
                            <h5 class="text-dark mb-3">Order Details</h5>
                            <div class="row">
                                <div class="col-6 text-start">
                                    <p class="mb-2">Order ID:</p>
                                    <p class="mb-2">Amount:</p>
                                    <p class="mb-2">Status:</p>
                                </div>
                                <div class="col-6 text-end">
                                    <p class="mb-2 fw-bold"><%= orderId %></p>
                                    <p class="mb-2 fw-bold">₹<%= amount %></p>
                                    <p class="mb-2 text-danger fw-bold"><%= status %></p>
                                </div>
                            </div>
                        </div>

                        <div class="d-grid gap-2">
                            <button class="btn btn-primary btn-lg mb-3" onclick="window.location.href='<%= retryUrl %>'">
                                Try Payment Again
                            </button>
                            <button class="btn btn-outline-secondary" onclick="window.location.href='/myOrder'">
                                View Orders
                            </button>
                        </div>

                        <div class="mt-4 pt-3 border-top">
                            <h6 class="text-muted mb-3">Need Assistance?</h6>
                            <div class="d-flex justify-content-center gap-3">
                                <a href="mailto:support@fitcart.com" class="text-decoration-none text-secondary">
                                    <i class="bi bi-envelope me-2"></i>support@fitcart.com
                                </a>
                                <span class="text-muted">|</span>
                                <a href="tel:1800XXXXXXX" class="text-decoration-none text-secondary">
                                    <i class="bi bi-telephone me-2"></i>1800-XXX-XXXX
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
